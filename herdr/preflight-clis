# Agent CLIs to keep updated before opening herdr. One per line.
# Optional: copy to ~/.config/herdr/preflight-clis to pin the list.
# Without this file herdr-preflight auto-detects: every agent kind herdr
# supports that is also installed on this machine.
agy
claude
devin
